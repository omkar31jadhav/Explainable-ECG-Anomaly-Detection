# pyache — ECG XAI helpers

Contains simple explainers and a runner to produce Grad-CAM and SHAP visualizations for the project's CNN heartbeat model.

Usage:

```
python -m pyache.run_examples --n-examples 20
```

Outputs are written to `results/xai/` by default.
